
<%- include('partials/header') %>
<div class="container mt-5 pt-5">
  <h1 class="text-success text-center">Prediction Result</h1>
  <div class="alert alert-success" role="alert">
    <h4 class="alert-heading text-center">Done!</h4>
    <p class="text-center">Your stroke risk prediction has been successfully calculated.</p>
    <div class="result-output p-3">
      <p><strong>Average Probability:</strong> <%= result.average_probability %></p>
      <p><strong>Risk Level:</strong> <%= result.rainbow_level %></p>
      <p><strong>Risk Percentage:</strong> <%= result.percentage_risk.toFixed(2) %>%</p>
    </div>

    <div class="risk-scale-container mt-4">
      <div class="risk-label-container mb-3">
        <span class="risk-label orange">Low Risk</span>
        <span class="risk-label yellow">Moderate Risk</span>
        <span class="risk-label blue">High Risk</span>
        <span class="risk-label violet">Very High Risk</span>
      </div>

      <div class="risk-scale position-relative">
        <div 
          class="risk-bar" 
          style="width: <%= result.percentage_risk %>%; background: linear-gradient(to right, #28a745, #8a2be2);">
          <span class="risk-percentage"><%= result.percentage_risk.toFixed(2) %>%</span>
        </div>
      </div>
    </div>

    <a href="/" class="btn btn-dark mt-4 w-100">Go Back</a>
  </div>
</div>

<style>
  .result-output {
    background-color: #f8f9fa;
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 5px;
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 20px;
    text-align: left;
  }

  .risk-scale-container {
    position: relative;
    width: 100%;
    margin-top: 20px;
  }

  .risk-label-container {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    font-size: 0.85rem;
    font-weight: bold;
    text-align: center;
    margin-bottom: 10px;
  }

  .risk-label {
    flex: 1;
    text-align: center;
    margin: 5px;
  }

  .risk-scale {
    width: 100%;
    height: 35px;
    background-color: #e0e0e0;
    border-radius: 20px;
    position: relative;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
  }

  .risk-bar {
    height: 100%;
    border-radius: 20px;
    text-align: center;
    color: white;
    font-weight: bold;
    line-height: 35px;
    position: relative;
    transition: width 1s ease-in-out;
    min-width: 30px;
    padding-left: 5px;
  }

  .risk-percentage {
    position: absolute;
    left: 5px;
    font-size: 0.9rem;
    color: #fff;
    font-weight: bold;
  }

  .btn {
    font-size: 1rem;
    padding: 12px 16px;
  }
</style>

<%- include('partials/footer') %>
